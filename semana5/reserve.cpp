#include <bits/stdc++.h>
using namespace std;

int n;
vector<int> x_;
vector<int> y_;

int main() {
    cin >> n;
    for (int i = 0; i < n; i++) {
        int x, y;
        cin >> x >> y;
        x_.push_back(x);
        y_.push_back(y);
    }
    double rmin = 0;
    double rmax = DBL_MAX;
    for (int i = 0; i < 80; i++) {
        
    }
}