#include <bits/stdc++.h>
using namespace std;

int n, k, q;
vector<int> bills;

int main() {
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        int bill;
        cin >> bill;
        bills.push_back(bill);
    }
    cin >> q;
    for (int i = 0; i < q; i++) {
        int amount;
        cin >> amount;
        // solve
    }
}