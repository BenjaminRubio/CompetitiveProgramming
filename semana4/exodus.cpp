#include <bits/stdc++.h>
using namespace std;

int N, K;
int roomsMask;

bool isPossible() {
    
}

int binarySearch() {
    int first = 0;
    int last = 0;
    while (first < last) {
        
    }   
}

int main() {
    cin >> N >> K;
    cin >> roomsMask;
}