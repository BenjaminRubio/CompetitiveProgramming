#include <bits/stdc++.h>
using namespace std;

int A, B;

bool isPossible() {
    
}

int binarySearch() {
    int first = 0;
    int last = A;
    while (first < last) {
        int mid = first + (last - first) / 2;
        
    }   
}

int main() {
    while (true) {
        cin >> A >> B;
        if (A == 0) {
            break;
        }
        cout << binarySearch() << '\n';
    }
}