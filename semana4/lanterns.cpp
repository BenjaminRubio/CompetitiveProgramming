#include <bits/stdc++.h>
using namespace std;

int N, L;
vector<int> lanterns;

bool isPossible(int maxCapacity) {
    
}

int binarySearch() {
    int first = 0;
    int last = L;
    while (first < last) {
        int mid = first + (last - first) / 2;
        
    }   
}

int main() {
    cin >> N >> L;
    for (int i = 0; i < N; i++) {
        int num;
        cin >> num;
        lanterns.push_back(num);
    }
}