#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (int)n; i++)

int T, a, b, c, d;

int main()
{
    cin >> T;
    while (T--)
    {
        cin >> a >> b >> c >> d;

        int x = b;
        int y = c;
        int z = c;

        cout << x << ' ' << y << ' ' << z << '\n';
    }
}